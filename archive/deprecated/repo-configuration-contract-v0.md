# Deprecated: Repository Configuration Contract v0

This file preserves the retired repository configuration contract for provenance.

Active authority now belongs to:

- `README.md` for navigation
- `GOVERNANCE.md` for lifecycle
- `ROUTING.md` for repository boundaries
- `docs/governance/single-source-of-truth.md` for authority mapping
- `docs/judgment-standard.md` for judgment output

## Migration note

The useful rule retained from this version is simple:

ITEM defines artifacts. ABYS builds execution machinery. SYNTEL carries protocol and signal infrastructure.

The long-form contract is no longer authoritative.
